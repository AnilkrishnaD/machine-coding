// import store

import Notifications from "../components/Notifications";

const state = {
  newNotifictaion: true,
};

const getAction = () => {};

const store = {
  Notifications: Notifications,
};

export default store;
